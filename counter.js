var counter = (arr) => {
  return `This array contains ${arr.length} elements`;
}

var sumArray = (arr) => {
  var sum = 0;
  arr.forEach((ele)=>{sum+=ele;})
  return sum;
}
console.log(module.exports);
